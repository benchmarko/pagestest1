{
  "name": "pagestest1",
  "version": "0.1.4",
  "description": "# pagestest1",
  "type": "commonjs",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist/src/* && rm dist/pagestest1.js",
    "copyXXX": "cp src/*.html dist/ && mkdirp -p dist/src && cp -r src/*.ts dist/src",
    "build": "tsc --build && npx rollup -c",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "@types/eslint__js": "^8.42.3",
    "@types/node": "^22.9.0",
    "eslint": "^9.14.0",
    "gh-pages": "^6.2.0",
    "mkdirp": "^3.0.1",
    "rollup": "^4.25.0",
    "rollup-plugin-typescript2": "^0.36.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.13.0"
  },
  "dependencies": {
    "ohm-js": "^17.1.0"
  }
}
